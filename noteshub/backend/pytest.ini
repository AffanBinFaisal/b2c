[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
testpaths = tests
markers =
    slow: inserts many documents (exclude with -m "not slow")
filterwarnings =
    ignore::DeprecationWarning:pydantic.*
    ignore::UserWarning:pytest_asyncio.*
